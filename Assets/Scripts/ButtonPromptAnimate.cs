using UnityEngine;
using System.Collections;

public class ButtonPromptAnimate : MonoBehaviour {

	// Use this for initialization
	void Start () {
		gameObject.GetComponent <Animator> ().enabled = true;
	}
	
	// Update is called once per frame
	void Update () {
	}
}
